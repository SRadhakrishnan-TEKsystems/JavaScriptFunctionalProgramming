# JavaScriptFunctionalProgramming

### Setup

    npm install @babel/core @babel/cli @babel/node @babel/preset-env

    npm install --save-dev jest jest-cli

    npx jest --init

Add babel.config.json
